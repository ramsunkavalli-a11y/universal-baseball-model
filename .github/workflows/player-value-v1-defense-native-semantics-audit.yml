name: Player Value v1 Defense native semantics audit

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip

      - name: Install project and frozen general-target transport
        run: >-
          python -m pip install --upgrade pip
          && python -m pip install -e ".[dev]"
          && python -m pip install "sportsdataverse==0.0.75"

      - name: Verify audit implementation
        run: python -m py_compile scripts/audit_player_value_v1_defense_native_semantics.py

      - name: Run pre-2025 native source-semantics audit
        run: python scripts/audit_player_value_v1_defense_native_semantics.py

      - name: Pin workflow metadata
        env:
          SOURCE_RUN_ID: ${{ github.run_id }}
          SOURCE_SHA: ${{ github.sha }}
        run: |
          python - <<'PY'
          import json, os
          from pathlib import Path
          path = Path('reports/generated/player-value-v1-defense-native-semantics-audit/report.json')
          data = json.loads(path.read_text())
          data['source_run_id'] = int(os.environ['SOURCE_RUN_ID'])
          data['source_sha'] = os.environ['SOURCE_SHA']
          path.write_text(json.dumps(data, indent=2, sort_keys=True) + '\n')
          PY

      - name: Upload audit artifact
        uses: actions/upload-artifact@v7
        with:
          name: player-value-v1-defense-native-semantics-audit
          path: reports/generated/player-value-v1-defense-native-semantics-audit/
          if-no-files-found: error
          retention-days: 30

      - name: Persist diagnostic result
        run: |
          set -euo pipefail
          cp reports/generated/player-value-v1-defense-native-semantics-audit/report.json \
            docs/player-value-v1-defense-native-semantics-audit-result.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/player-value-v1-defense-native-semantics-audit-result.json
          git commit -m "Persist Defense native semantics audit"
          for attempt in 1 2 3; do
            git fetch origin source-certification-poc
            if git rebase origin/source-certification-poc && git push origin HEAD:source-certification-poc; then exit 0; fi
            git rebase --abort || true
            sleep $((attempt * 2))
          done
          exit 1
