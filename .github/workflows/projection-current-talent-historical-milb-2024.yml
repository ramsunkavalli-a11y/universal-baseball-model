name: Projection reuse Current Talent historical MiLB 2024

on:
  push:
    branches:
      - source-certification-poc
    paths:
      - ".github/workflows/projection-current-talent-historical-milb-2024.yml"
      - "scripts/materialize_current_talent_historical_milb_game_evidence.py"
      - "src/universal_baseball/current_talent_era.py"
      - "src/universal_baseball/current_talent_identity_corrections.py"
      - "src/universal_baseball/current_talent_milb_source.py"
      - "src/universal_baseball/current_talent_milb_evidence.py"
      - "src/universal_baseball/current_talent_official_outcomes.py"
      - "src/universal_baseball/current_talent_season_reconciliation.py"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  materialize:
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        level: [aaa, aa, "a+", a, rk]
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip

      - name: Install project
        run: python -m pip install --upgrade pip && python -m pip install -e ".[dev]"

      - name: Run historical evidence unit tests
        run: >-
          python -m pytest
          tests/test_armstjc_contacts.py
          tests/test_current_talent_era.py
          tests/test_current_talent_identity_corrections.py
          tests/test_current_talent_milb_source.py
          tests/test_current_talent_milb_evidence.py
          tests/test_current_talent_official_multiteam.py
          tests/test_current_talent_official_outcomes.py
          tests/test_current_talent_participant_authority.py
          tests/test_current_talent_season_reconciliation.py
          tests/test_official_mixed_codes.py
          tests/test_season_stat_assets.py

      - name: Materialize 2024 historical Current Talent game evidence
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: >-
          python scripts/materialize_current_talent_historical_milb_game_evidence.py
          --season 2024
          --level "${{ matrix.level }}"

      - name: Upload accepted 2024 historical game evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: current-talent-historical-milb-full-season-2024-${{ matrix.level }}
          path: reports/generated/current-talent-historical-milb-game-evidence/2024/${{ matrix.level == 'a+' && 'aplus' || matrix.level }}/
          if-no-files-found: warn
          retention-days: 14
