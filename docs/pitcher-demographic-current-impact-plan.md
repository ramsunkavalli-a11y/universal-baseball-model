# Pitcher demographic current-impact plan

Status: accounting audit; no selection decision.

Using the same current inputs, compare the promoted private build with an explicit
counterfactual that disables only the frozen pitcher age-for-level/hand adjustment.
Reapply the identical nested career workload calculation to the counterfactual and
report total expected WAR, FV threshold counts, grade changes, and largest player
changes. Verify that hitter outputs do not change.

This audit cannot retune coefficients, thresholds, workload, role, or probabilities.
